var primary = localStorage.getItem("primary") || '#00cc66 ';
var secondary = localStorage.getItem("secondary") || '#39aeff ';

window.pocoAdminConfig = {
	// Theme Primary Color
	primary: primary,
	// theme secondary color
	secondary: secondary,
};